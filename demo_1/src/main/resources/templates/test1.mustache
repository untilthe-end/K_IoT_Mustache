<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
    <h1>여기는 test1 파일이야</h1>
    <!-- 머스태치 주석 문법 -->
    {{! 변수를 출력 }}
    <p> 문자열 출력: {{stringValue}}</p>
    <p> 정수값 출력: {{intValue}}</p>

    {{! 섹션 문법 - if 구문에 참 또는 반복문 실행}}
    {{#hasData}}
        여기는 참이면 화면에 보여진다.
    {{/hasData}}

    {{! 부정 섹션 문법 }}
    {{^hasData}}
        거짓이면 화면에 보여짐
    {{/hasData}}

    <hr>
    {{#items}}
        {{! . 은 키 값이 없을 때 사용한다 }}
        {{.}}
    {{/items}}
    <br>
    {{#map}}
        {{key1}}
    {{/map}}

    {{map.key2}}
</body>
</html>